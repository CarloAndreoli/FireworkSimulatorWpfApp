namespace fWorks
{
    // Intentionally left as a compatibility placeholder.
    // Safe to delete from the project if it is no longer referenced.
    internal static class StageCompatibilityPlaceholder
    {
    }
}
